import { ForcedFocusDirective } from './forced-focus.directive';

describe('ForcedFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ForcedFocusDirective();
    expect(directive).toBeTruthy();
  });
});
